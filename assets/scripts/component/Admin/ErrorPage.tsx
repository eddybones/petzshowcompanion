import React = require('react');
import { ReactElement } from 'react';

export default function ErrorPage(): ReactElement {
    return (<p>There was an error, but you probably already knew that.</p>);
}